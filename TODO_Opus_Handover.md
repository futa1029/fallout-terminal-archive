# Fallout 76 Lore Archive - Handover to Opus (Next Steps & Fixes)

Opusへ作業を引き継ぐための状況説明と、次回以降のタスクリストです。
次回Opusとのセッションを開始した際、このファイル（`TODO_Opus_Handover.md`）を読み込ませるか、内容をそのままプロンプトとして貼り付けることで、現在までの経緯と完璧な再結線が可能です。

---

## 1. 現在のリポジトリ状態と発生した問題
- **現在の状態:** 全1,310件の新規記事生成（Phase 1〜4）を完了後、インデックス管理スクリプト（`remove_duplicates.js`等）の設計ミスにより「本来ドラフトであるべき記事が一斉に公開されてしまう」事故が発生。現在は `git revert` を用いて、当該Pushの直前の安全な状態（1310件すべてが非公開状態）へ完全に巻き戻し済み。
- **問題① ゴミデータの混入:** Fandom Wikiからのスクレイピング時にCloudflareのBot対策画面を取得してしまい、「Just a moment...」というタイトルのエラーHTMLファイルが数点混入している（`nw.html`, `buds_full.html`, `f76.html` など）。
- **問題② メタページのリスト混入:** 「攻略ガイド」「Resources」「Season」「Rules」といった本来記事一覧に表示すべきではないUI・管理用のページがリストに「ドラフト」として紛れ込んでいる。
- **問題③ 翻訳の端折りと不完全さ:** 大量の自動生成バッチ処理を実行した際、LLM（Gemini）のコンテキスト制限等により、長文ロアの翻訳が省略（端折り）されたり、不完全なまま生成されている記事が多数見受けられる。

## 2. Opusに依頼する最優先タスクリスト

### タスクA: セキュアで実用的な管理画面（`admin.html`）の再構築
1. **認証のセキュリティ強化:**
   - URLパラメータ（`?key=...`）による脆弱なGET認証を完全に廃止。
   - ログインフォーム画面（POST）を設け、内部的なパスワードのハッシュ化や `localStorage/sessionStorage`、あるいはサイトに導入済みの Supabase Auth などを活用したセキュアな認証ロジックへ根本から作り直すこと。
2. **一覧とカテゴリフィルターの導入:**
   - 現在の「Draft（未公開）リスト」のみを表示する仕組みを廃止。
   - 全ての記事（公開済み＋非公開、約1500件以上）を一つのダッシュボードで統合管理し、カテゴリ（人物、場所、勢力など）で即座に絞り込めるフィルター機能を実装すること。
3. **公開/非公開のトグル切り替え:**
   - 各記事ごとに「公開 (Published) ⇔ 非公開 (Draft)」を1クリックで切り替えられるトグルボタン式のUIを実装。
   - スクリプト運用との整合性を保ちつつ、サイトデータの安全な上書き更新ができる管理フローを確立すること。

### タスクB: リストのクリーンアップと修正
1. **不要データの完全削除:**
   - 「Just a moment...」というタイトルの記事（3件）、および「fo76-guide」「resources」「season」「rules」「contact」などのシステムメタページをファイルおよびインデックスリスト（`remove_duplicates.js` など）から完全に除外・削除する。
2. **既存記事の完全な復旧:**
   - 元々公開されていた記事（エンクレイヴ、Vault76など）にかかっている不要な `status: "draft"` を、新しい安全な仕組みのもとで解除し公開状態に戻す。

### タスクC: 不完全な翻訳記事の再構築と修復（最重要）
1. **不完全リストの抽出:**
   - 生成された1,310個のHTMLファイルの中から、ファイルサイズが極端に小さいものや、本文内に「省略」などのワードが含まれる不完全な記事をスクリプトで洗い出す。
2. **Opusによる高精度な再翻訳:**
   - 抽出された記事に対して、**Opusの極めて高い長文理解・翻訳能力を使い、一切の端折り（省略）を許さずに完全な翻訳を行う。**
   - 既存の「Falloutターミナル（`term-container`など）」のHTML・CSS形式に完全準拠し、ロア・アーカイブとして相応しい高品質な記事へと作り直す。

---
**※Opusへのメッセージ※**
Geminiからの引き継ぎとなります。前回のシステム設計上のミス（一括処理による巻き込みと翻訳の省略）により、ユーザーに多大な不安を与えてしまいました。
今後の作業は、決して一気にバッチ処理を強行せず、まずは「タスクA（管理画面の堅牢な基盤作り）」をユーザーと確認しながら着実に進め、その後「タスクB」「タスクC」へと1つずつ確実・丁寧に進めてください。ロアの翻訳品質とシステムのセキュリティが最優先です。よろしくお願いします。
